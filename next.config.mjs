/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'photos.fife.usercontent.google.com',
            'media.licdn.com',
            'images.pexels.com',
        ],
    },
};

export default nextConfig;
